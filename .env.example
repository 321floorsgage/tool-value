# Public browser configuration only. Both values are intentionally public and
# are constrained by Supabase grants and row-level security.
# NEVER add a service_role key, sb_secret_ key, database password, or access token.
VITE_SUPABASE_URL=https://dbqyluplqlrvfpldbeay.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_OfHjGLy3-PkQIREEh1TNMg_1eRWIsLV

# Scan Tool (Netlify Function only — never a VITE_ variable):
#   AI_VISION_MODEL   optional, defaults to gpt-4o-mini
# The AI credentials come from Netlify AI Gateway at runtime. Do not add an
# OpenAI key here, and never prefix an AI key with VITE_.
